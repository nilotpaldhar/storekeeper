import Alert from '@ui/feedback/Alert';

/**
 * Render the PlaceHolder component.
 *
 * @return {Element} The PlaceHolder component.
 */
const PlaceHolder = () => (
	<Alert>This payment gateway is currently under development and will soon be ready for use.</Alert>
);

export default PlaceHolder;
