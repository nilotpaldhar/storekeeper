import { useSearchBox } from 'react-instantsearch-hooks-web';

/**
 * Render the VirtualSearchBox component.
 *
 * @return {Element} The VirtualSearchBox component.
 */
const VirtualSearchBox = () => {
	useSearchBox();

	return null;
};

export default VirtualSearchBox;
